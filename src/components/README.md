# Components Directory

This directory contains all React components for the S3 File Manager application.

## Structure
- `ui/` - Reusable UI components (buttons, forms, etc.)
- `auth/` - Authentication related components
- `upload/` - File upload components
- `s3/` - S3 bucket and object management components